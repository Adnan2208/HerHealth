# Keep cloud images slim: opencv-headless (not opencv-python), no matplotlib.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
numpy==1.26.4
pillow==11.1.0
opencv-python-headless==4.10.0.84
tensorflow==2.19.0
# NOTE: TF is ~500MB. Required for real inference. For UI-only testing
# without TF, run with ANEMIA_MOCK=1 (no TF import attempted).
